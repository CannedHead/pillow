doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    title PILLOW EDUCATION 

    meta(name="description",content="pillow consulting -sigue tus suenos")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

    meta(property="og:type" content="website")
    meta(property="og:title" content="Siembra un arbol.")
    meta(property="og:image" content="http://s5.postimg.org/nt23g0hjb/logo.png")
    meta(property="og:description" content="Suena con nosotros.")
    meta(property="og:url" content="http://www.consultyourpillow.com")

    link(href="favicon.png" rel="shortcut icon" type="image/png")  
    link(href="/css/bootstrap.css", rel="stylesheet")
    link( rel="stylesheet" href="/css/normalize.min.css" type="text/css")
    link( rel="stylesheet" href="/css/grid.min.css" type="text/css" )
    link( rel="stylesheet" href="/css/style.min.css" type="text/css")
    link(href="/css/styles.css", rel="stylesheet")

    script(src="//ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js", type="text/javascript")
    script(src="/js/bootstrap.min.js", type="text/javascript")
    script(type="text/javascript" src="/js/jquery.min.js")
    script(type="text/javascript" src="/js/jquery-ui.min.js")
    script(type="text/javascript" src="/js/form.min.js")
    script(type="text/javascript" src="/js/wide-video.js")    
    
  body
    
    div#video(style="z-index:-3") 
    div#images(style="z-index:-3")

    img.col-xs-12.col-md-3.logo.center-block(src="/img/pillow_logo-02.svg")
    p.hero(style='font-weight:900;color:font-color:#4F4F50') Education will set you FREE
    p.col-md-4.col-md-offset-4.intro Pillow Education es una propuesta que busca apoyar a instituciones educativas a cumplir su misión formadora, tan importante en el mundo de hoy, otorgándole a los docentes, padres de familia y estudiantes conocimientos y herramientas en las temáticas de:

    .col-md-4.col-md-offset-4(style='text-align:center')

        h2.texto(style='margin-top: 0%') TRABAJO EN EQUIPO
        hr(style = 'border-color:#C5196E' )
        h2.texto(style='margin-top: -2%') CREATIVIDAD
        hr(style = 'border-color:#C5196E' )
        h2.texto(style='margin-top: -2%') INNOVACIÓN
        hr(style = 'border-color:#C5196E' )
        h2.texto(style='margin-top: -2%') EMPRENDIMIENTO
        hr(style = 'border-color:#C5196E' )

    p.col-md-4.col-md-offset-4.intro(style='margin-top: -0.5%') El objetivo principal de Pillow Education es desarrollar en los estudiantes estás habilidades, buscando que ellas les permitan cumplir sus metas y enfrentarse al mundo que está por venir.
    


    .container
        .row
            .col-md-6.col-md-offset-3
                form.formulario(method="post" action= "/contact")
                     
                    .form-group
                        input.form-control(type="text" name="nombre" placeholder="Nombre" required)
                    br
                    .form-group
                        input.form-control(type="email" name="email" placeholder="E-mail" required)
                    br
                    .form-group
                    input.boton.form-control(style='margin-bottom: 3%' type="submit" value="RECIBE  MÁS INFORMACIÓN" )

                    p(style='text-align:cenfont-ter; font-color:#333') Con el apoyo de: 
                    a(href='http://www.imagine.cc' target="blank")
                        img.col-xs-12.col-md-5.logo.center-block(src="/img/Logo_Imagine.png")
                        br 
                        br

                    
        
        script(src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js")
        script(src="//www.google-analytics.com/analytics.js" async="")
            
        script.
            $(document).ready(function() {
                $("#loader").fadeOut();
                $("#loading").fadeOut();
            });
            
            // Remove no-script background image
            document.body.style.backgroundImage = 'none';

            // Avoid cropped background when the page has y-overflow
            fixBackgroundHeight();

            window.onresize = function()
            {
                var wh = $(window).height();
                var ww = $(window).width();
                var ratio = wh/ww;

                // Avoid cropped background when the page has y-overflow
                fixBackgroundHeight();

                // Resize the background items
                if(!mobileCheck())
                {
                    sizeVideoBg(ratio);
                    sizeVideo(ratio);
                }
                else
                {
                    sizeImage(ratio);
                }
            };

            // Load first video
            loadMediaFirst();

            // Waits till the page ends loading
            $(window).bind('load', function()
            {
                // Avoid cropped background when the page has y-overflow
                fixBackgroundHeight();

                // Start to load the assets
                loadMediaSecond();

                setTimeout("first_slide_ocurred = true;", 4300);

                setTimeout(function()
                {
                    if(first_video_loaded && second_video_loaded)
                    {
                        console.log('Enough bandwidth to load videos');
                        enough_bw = true;
                    }
                    else
                    {
                        $('video').html('');
                    }
                }, 4600);

                if(!mobileCheck())
                {
                    loopVideo();
                }
                else
                {
                    loopImage();
                }
            });
            
            (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
            (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
            m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
            })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

            ga('create', 'UA-50689634-1', 'consultyourpillow.com');
            ga('send', 'pageview');

                     